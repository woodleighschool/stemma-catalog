# yaml-language-server: $schema=https://raw.githubusercontent.com/woodleighschool/stemma/refs/heads/main/stemma.schema.json
apiVersion: stemma/v1alpha1
kind: MacSoftware
metadata:
  name: rosetta
spec:
  destinations:
    woodstar:
      pkginfo:
        category: System
        description: Installs Apple's Rosetta translation software on Apple silicon Macs.
        developer: Apple
        installcheck_script: |
          #!/bin/sh
          [ "$(/usr/bin/arch)" = arm64 ] || exit 1
          /usr/bin/arch -x86_64 /usr/bin/true 2>/dev/null && exit 1
          exit 0
        installer_type: nopkg
        minimum_os_version: "11.0"
        name: Rosetta
        postinstall_script: |
          #!/bin/sh
          /usr/sbin/softwareupdate --install-rosetta --agree-to-license
        unattended_install: true
        uninstallable: false
        version: "1.0"
  extends: mac-software
