# yaml-language-server: $schema=https://raw.githubusercontent.com/woodleighschool/stemma-catalog/main/stemma.schema.json
apiVersion: stemma/v1alpha1
kind: MacSoftware
metadata:
  name: google-chrome
spec:
  extends: mac-software
  source:
    url: https://dl.google.com/dl/chrome/mac/universal/stable/gcem/GoogleChrome.pkg
  application:
    bundle_id: com.google.Chrome
  signature:
    signer: apple:developer-id:EQHXZ8M8AV # Google LLC
  icon: google-chrome
  destinations:
    woodstar:
      pkginfo:
        category: Productivity
        description: >-
          Get more done with the new Google Chrome. A more simple, secure, and
          faster web browser than ever, with Google's smarts built-in.
        developer: Google
        name: Google Chrome
        unattended_install: true
        unattended_uninstall: true
        uninstall_method: removepackages
        uninstallable: true
      targets:
        include:
          - label_name: All Hosts
            actions:
              - managed_installs
        exclude:
          - label_name: SAC MacBooks
